
import AuthRegisterPage from '@/features/auth/components/auth-register.page';

const UserRegisterPage = () => {
    return (
        <div>
            <AuthRegisterPage role={"user"}/>
        </div>
    );
}

export default UserRegisterPage;
